{
  "name": "probaboracle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tiny useless oracle chatbot.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
