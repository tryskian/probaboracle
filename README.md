# Probaboracle

Fresh bootstrap.

This repository has been re-initialised from a clean slate. Keep the shape
small and add only what the app actually needs.

Current status:

- git repo initialised
- no runtime or tooling scaffold yet
- next step is the first real product slice
